# ============================================================
# Variables de entorno para el Backend Despachos
# INSTRUCCIONES:
#   1. Copia este archivo: cp .env.example .env
#   2. Rellena los valores reales
#   3. NUNCA subas el archivo .env a Git (está en .gitignore)
# ============================================================

# Base de datos MySQL
DB_NAME=despachos_db
DB_USERNAME=devops_user
DB_PASSWORD=CambiameAhora123!

# El .env es solo para desarrollo local
# En producción (EC2), los valores se pasan como secrets de GitHub Actions
